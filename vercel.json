{
    "version": 2,
    "builds": [
      {
        "src": "app.py",
        "use": "@vercel/python",
        "config": {
          "buildCommand": "python3 -m venv myenv && source myenv/bin/activate && pip install --upgrade pip && pip install -r requirements.txt && python3 app.py"
        }
      }
    ],
    "routes": [
      {
        "src": "/(.*)",
        "dest": "app.py"
      }
    ]
  }
  