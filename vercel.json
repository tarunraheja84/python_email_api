{
    "version": 2,
    "builds": [
      { "src": "app.py", 
        "use": "@vercel/python" ,
        "commands": {
          "build": "python3 -m venv venv && source venv/bin/activate && pip install --upgrade pip && pip install flask && python3 app.py" 
        }
      }
    ],
    "routes": [
      { "src": "/(.*)", 
        "dest": "app.py" 
      }
    ]
}
  